# 安全与隐私

## 本工具的权限边界

这是与 OpenAI 无隶属关系的本地 macOS 程序。它通过本机官方 Codex 的 App Server 查询额度。发布包不含开发者账号材料，使用者需要自行登录自己的 Codex。

小工具本身仅发送 `initialize`、`initialized` 和 `account/rateLimits/read`，接受额度或账号状态通知。它不请求对话、模型调用、购买、兑换或上传文件。这个限制由本项目代码实现，**不是对 Codex 凭据或子进程施加的权限沙箱**。

本项目不直接读取登录令牌、Cookie、钥匙串或聊天记录，不将额度写入磁盘，也没有开发者服务器、网络监听、自动更新或遥测客户端。诊断参数 `--probe` 会实时查询并将额度摘要打印给调用终端；不要把自己的诊断输出直接公开。

外部 Codex 是独立的可信依赖：它负责认证、网络访问和自身状态管理，仍可能读取或写入自己的配置、凭据和日志。用户配置可能影响其行为。本项目没有审计整个 Codex 或远端服务。

## 已实施的防护

- 对 App bundle 及内部 Codex 可执行文件验证 Apple 信任链和 OpenAI Team ID，并匹配 Codex bundle ID。
- 固定可发现的 App 路径；不启动 PATH 中的同名任意程序。
- 直接使用参数数组启动子进程，不拼接 shell 命令。
- 继承环境采用白名单；不传入启动终端的 API Key、动态库注入变量或自定义端点变量。
- 子进程在 `/` 下启动，避免项目目录配置；设置 `analytics.enabled=false`。
- 单次查询 30 秒超时、一次只执行一个刷新、最多 4 MB 未处理管道缓冲。
- 响应按请求 ID 关联，以强类型模型读取需要的额度字段；其他身份字段不进入额度模型。
- 不持久化额度；收到账号状态通知时清空显示。其他进程切换登录不保证即时推送，建议切换账号后重启。
- 登录启动由用户选择并交给系统审批。
- 构建使用临时私有编译缓存和源路径映射，发布清单排除本机配置、日志、凭据、测试记录及 Git 元数据。

## 公开前检查

2026-09-17 对初始版本进行了独立静态基线审查、架构检查及主审复核，并对公开衍生版再次复核。未发现有源码证据支持的凭据泄露或越权漏洞；发现并清理了不适合公开的个人化标识、安装路径和真实额度记录。

公开版取消了旧版落盘额度缓存，新增签名验证与环境白名单，并重新构建，进行了离线自检、实际读取测试、签名和架构检查、源代码与二进制敏感模式扫描、公开包文件清单检查。

这是一轮有限范围检查，**不是第三方安全认证，也不保证不存在漏洞**。未审计 macOS、Swift 编译器、整个 Codex 实现和远端接口。Intel 版本未做实机运行测试。下载版没有 Developer ID 签名或 Apple 公证；临时签名不能证明公开发布者身份。

## 报告问题

普通兼容性问题可提交 Issue。涉及凭据或敏感信息时，先只描述影响和最小复现条件，不要公开令牌、Cookie、账号 ID、完整配置、聊天记录或原始诊断输出。请等待维护者提供适当的私密沟通渠道后再提交敏感细节。
